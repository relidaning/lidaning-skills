{
  "mcpServers": {
    "obsidian": {
      "type": "http",
      "url": "${OBSIDIAN_MCP_URL}/mcp/",
      "headers": {
        "Authorization": "Bearer ${OBSIDIAN_MCP_TOKEN}"
      }
    },
    "rag": {
      "type": "http",
      "url": "http://localhost:8081/mcp"
    }
  }
}
